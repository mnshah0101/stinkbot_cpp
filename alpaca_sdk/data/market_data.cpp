#include "market_data.h"

void MarketData::getMarketData(const std::string &apiKey, const std::string &apiSecret, const std::string &symbol)
{
    std::cout << "Fetching market data for " << symbol << " with API Key: " << apiKey << std::endl;
}
